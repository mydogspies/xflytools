package com.mydogspies.xflytools.controller.elements;

import javafx.scene.control.TextField;

/**
 * A custom text field so that we can easily make custom
 * css labels and differentiate between types without hacking stuff
 * @author Peter Mankowski
 * @since 0.3.0
 */
public class AutoPilotField extends TextField {

    public AutoPilotField() {
        super();
    }
}
